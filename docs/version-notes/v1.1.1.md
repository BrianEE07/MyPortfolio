# v1.1.1

## Summary
This version cleaned up repo-facing documentation links and tightened the `Stock Details` presentation without changing the holdings model or tab structure.

## Highlights
- Replaced machine-specific absolute filesystem links with GitHub-friendly relative Markdown links.
- Tightened the `Stock Details` layout with smaller section spacing and denser table styling.
- Added a compact ranking badge in the symbol column.
- Preserved the existing bilingual labels and overall data scope.
- Regenerated `docs/index.html` and `docs/static/styles.css` so published output matched the updated UI.

## Verification
- Confirmed repo-facing documents no longer contained absolute `/Users/...` links.
- Rebuilt the static site successfully with `python portfolio.py --output docs/index.html`.
- Verified `GET /` and `GET /health` still returned `200`.
