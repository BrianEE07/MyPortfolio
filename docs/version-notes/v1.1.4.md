# v1.1.4

## Summary
This version replaced the fixed hero subtitle with a server-rendered progress line toward the long-term wealth goal.

## Hero Progress Subtitle
- Reduced the `Wei's Portfolio` hero title size across desktop and mobile layouts.
- Replaced the static subtitle with:
  - `邁向財富自由之路的第 X 天｜Y% · $CURRENT / $1,000,000`
- Counted Day 1 from the first US stock purchase date `2026-04-02`.
- Calculated progress toward the fixed `USD 1,000,000` goal from current total market value.
- Kept the subtitle Chinese-only and server-rendered.
- Added the fallback subtitle:
  - `邁向財富自由之路的第 X 天｜進度暫不可用`
- Split the subtitle into structured lead/progress parts so narrow layouts could stack them when needed.

## Verification
- Rebuilt the static site successfully with `python portfolio.py --output docs/index.html`.
- Verified the Flask app still served `GET /` and `GET /health` successfully.
